version: '2'
services:
  flask:
    build: ./flask
    ports:
     - "5000:5000"
    volumes:
     - .:/work
    tty: true
    depends_on:
     - redis
    links:
     - redis
  redis:
    image: redis:3.0.7